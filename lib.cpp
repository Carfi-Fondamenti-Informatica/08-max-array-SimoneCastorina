#include "lib.h"
float numeri (float arr[],int n)  {
        for (int i = 0;i<n;++i) {
            if(arr[0]>arr[i]){

        }else{
                arr[0]=arr[i];
            }

        }
        return arr[0];
}
